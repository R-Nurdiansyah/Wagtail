name: minimap2
channels:
  - bioconda
  - conda-forge
  - nodefaults
dependencies:
  - mappy
  - singlem
  - pip
  - pip:
    - polars==0.18.4 #polars v0.20.3 in conda is buggy and it only works with 0.18.4 VERSION
